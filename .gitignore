node_modules
dist
*.log
.firebase
.DS_Store
functions/lib
functions/node_modules
serviceAccountKey.json

# Tauri / Rust
src-tauri/target
src-tauri/gen
src-tauri/Cargo.lock

# Built installer binary (generated; do not commit — ~10MB)
public/downloads/VERGR-Setup.exe
public/downloads/VERGR-Setup.msi

# KYC private key material — never commit
kyc-keys.tmp
kyc-priv*.pem
*.pem
