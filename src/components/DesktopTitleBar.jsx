/**
 * DesktopTitleBar is no longer needed — native window decorations
 * handle minimize/maximize/close/drag. This file is kept so
 * existing imports don't break.
 */
export default function DesktopTitleBar() {
  return null;
}