export const compressImage = async (file, maxW=1920, q=0.8) => new Promise(r => { const img=new Image(); img.onload=() => { const c=document.createElement('canvas'); let w=img.width,h=img.height; if(w>maxW){h=(maxW/w)*h;w=maxW;} c.width=w;c.height=h; c.getContext('2d').drawImage(img,0,0,w,h); c.toBlob(r,'image/jpeg',q); }; img.src=URL.createObjectURL(file); });
export const compressVideo = async (f) => f;
export const getMediaDimensions = (f) => new Promise(r => { if(f.type.startsWith('video')){const v=document.createElement('video');v.onloadedmetadata=()=>r({width:v.videoWidth,height:v.videoHeight,duration:v.duration});v.src=URL.createObjectURL(f);}else{const i=new Image();i.onload=()=>r({width:i.width,height:i.height});i.src=URL.createObjectURL(f);}});
export const compressMedia = async (file, tier='free') => { const mx={free:25e6,lite:100e6,pro:500e6}; if(file.size>(mx[tier]||mx.free)) throw new Error('File too large for your tier'); return file.type.startsWith('image') ? compressImage(file) : file; };
